Ziel

Das Backend liest Daten aus der Datenbank und stellt sie per API bereit.


Anforderungen / Inhalt

Backend verbindet sich mit der DB über Umgebungsvariablen

Mindestens ein HTTP-Endpoint, z.B.:

GET /api/items

Endpoint liefert DB-Daten als JSON

Fehlerhandling bei nicht erreichbarer DB


Erforderliche Git Commits

Git Message: "Task 3: Backend DB Integration"

Enthält Backend-Code für:

DB-Verbindung

SQL/ORM-Query

API-Endpoint



Erfolgskriterium

API-Endpoint liefert echte DB-Daten 